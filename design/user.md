# お願いするときのフォーマット

ツールの現状
design/claude.md

やってほしいこと
win5のウインドウをもう少し大きくしてください。
win5に「timestrech」という項目を足し、
50%から100%で、指定できて、50%にしたら各モーラの長さが半分になります
タイムストレッチの仕組みは下記です
- **soundtouchjs `^0.2.3`**（ピッチを変えないタイムストレッチ）
ここで大事なのが、各モーラごとがタイムストレッチされるので、
全体が50%になってオーディオ全体の長さが変わるわけではないという点です
なので、聴覚的にはぶつ切りになります。
パーセントを入力できて、適用とクリアボタンをつける感じ
これらはリアルタイムである必要はなし
win3で、現在スライスごとの音を確認できる仕様だが、タイムストレッチをしたら
クリックしたときにそのときの音を確認できる必要あり
タイムストレッチは元のオーディオに対して行う必要あり(聴覚的な音質の劣化を防ぐ)
また、win5にフェードイン、フェードアウトのボタンがあるが、
これは聴覚的にどっちを先にかけた方が良いのかは、ちょっとそちらの意見をください

やらない事
依存関係的に関係のないファイルは読まない

まだコードを書かないで、実装できるかの検証と、どれくらいの工程かを教えてください。

# 継承フォーマット

一度claude codeを閉じるから、また新規のclaude codeが見ても今のツールの現状が分かるように、
最小プロンプト(design/claude.md)を作成/更新してほしい